# template
cpp template repository

## todo

[] cookie cutter generate repository

## modern use

### Cpp language server

please use clangd

``` shell
# ubuntu
$ sudo apt update
$ sudo apt upgrade
$ sudo apt install clangd

# arch
$ sudo pacman -Syy
$ sudo pacman -S clangd
```
